# 验收报告

时间：2026-07-21（Asia/Shanghai）

## 结果

Codex 墨水屏监控已经完成、安装并作为当前用户登录任务运行。方案未刷固件，使用设备现有 BLE 图片协议。

## 真机证据

只读状态探测成功：

- 设备：`SKD-CLOCK`
- 分辨率类型：0
- 实际分辨率：212×104
- 颜色：单色（`is_bwr=false`）
- 固件：1.8
- 电压：测试期间 3.403–3.487 V
- 温度：29–30 °C
- 蓝牙常开：是
- 已激活：是

受控上传成功：

- 单帧数据：5 B 厂商头 + 216×104 行对齐像素数据
- 分片：22 个 129 B 数据帧，命令按 `60/61` 成对交替
- 提交：1 个 129 B `62` 帧
- 总写入：23 帧
- 最后一次竖版状态条写入：2026-07-21 20:41:08，强制发送 23 包成功，无 GATT 错误

厂商图像协议没有应用层 CRC、ACK 或“刷新完成”回执，因此可验证的边界是：状态读取、MTU 检查、全部 GATT Write With Response 和提交命令均成功。实体屏最终像素只能由目视确认；程序保存的同源位图为 `previews/live.png`。

## 自动运行证据

- Windows 任务：`Codex E-Ink Dashboard`
- 触发：当前用户登录 + 每分钟看门狗
- 状态：Running
- 单实例：是
- 电池供电：允许启动，切换到电池时不停任务
- 失败重启：最多 3 次，每次间隔 1 分钟
- 睡眠策略：不唤醒、不重启电脑；唤醒后补跑
- 00:09:12 日志记录 `unchanged`，验证了相同位图时零上传路径
- 01:02:05 修复后完成一次 23 包真实蓝牙上传
- 01:02:51 手动停止任务，01:03:28 被看门狗自动拉起，01:03:35 正常完成 `unchanged` 检查
- 竖版部署：状态探测读取电压 3.487 V；15:06:45 强制上传 23 包成功；后台在 15:08:02、15:09:13 连续上传成功
- 竖版右上角改版：18:32:43 上传 23 包成功；显示周额度重置日期 `M.D`，API 计划显示 `API`
- 竖版告警摘要改为 10px：18:38:03 上传 23 包成功；长摘要保持单行并在右侧截断
- API 账户判定修复：实时 Codex 接口明确返回 ChatGPT 额度鉴权不可用，识别为 API；部署配置设为 `account_mode: "api"` 以避免瞬时失败回退到历史日期。19:10 强制上传 23 包成功，任务恢复为 `Running`。
- API 额度区调整：19:19 强制上传 23 包成功；API 模式保留右上角 `API`，移除 `Q --` 和额度条，任务区坐标保持不变。
- API 紧凑任务区：19:22 强制上传 23 包成功；任务区贴到标题栏下方，非 API 坐标不变。
- 竖版状态条：20:03 强制上传 23 包成功；底部最多显示四项，`update_plan` 使用真实步骤分数，其余运行、未读完成和失败状态使用共享 7×7 像素符号。

## 自动化验证

- `unittest`：65 项通过，0 失败
- Python 编译检查：通过
- 依赖一致性：`No broken requirements found`
- 四种协议分辨率包数：BW 23/33/39/119，BWR 47/67/79/239，均通过 golden tests
- 四种 UI 分辨率：212×104、250×122、296×128、400×300 均生成准确尺寸的纯黑白位图
- 视觉检查：无重叠、裁切、溢出或灰阶抗锯齿像素
- 状态机覆盖：运行、完成、错误、中断、陈旧任务、subagent 排除、重复告警、跨月目录、当前轮 `update_plan` 进度、未读完成和 `task_complete.error`
- BLE 安全门覆盖：短状态包、未知分辨率、分辨率冲突均在任何图像写入前失败

## 刷新策略

- 活跃时每 30 秒采集；空闲时每 60 秒采集
- 任务完成/失败最迟约 60 秒显示
- 项目顺序稳定，避免仅因后台活动时间改变而刷新
- 竖版不显示 `SYNC`、重置图标或具体时间；右上角只显示周额度重置日期 `M.D`
- 额度显示实时接口返回的精确整数百分比
- **内容优先路径（2026-07-21 加固）**：位图哈希相同则不扫描蓝牙、不写设备；历史上曾误在每次轮询先 `probe`，导致无意义连接与电压抖动上传
- 中途失败时重新连接并从整帧开头重传（`retries=2`）
- 竖版活动时每 30 秒采集，空闲时每 60 秒采集；只在位图变化时写入屏幕
- 状态条只保留四格：真实计划进度优先，否则为运行符号；随后是未读完成对勾和失败叉号。三个符号来自固定 7×7 位图表，不依赖 Unicode 字体。
- 已读的完成或失败历史任务不会进入状态条，也不会让顶部保留 `DONE` 或 `ERR`。

## 即时更新（2026-07-22）

- 新增本地 Codex 状态文件监听；会话、现有任务标题索引和未读标记变化会在 1 秒静默合并后立即重新计算位图。
- 图片哈希不变时维持零 BLE 上传；上传过程中抵达的新事件不会丢失。
- `state_5.sqlite`、`logs_2.sqlite` 及其 WAL/SHM 文件纳入监听；仅数据库事件的刷新会跳过实时额度读取，避免额度读取自身写库形成自触发循环。
- 任务标题只来自 Codex 现有侧栏标题；无标题固定显示“未命名任务”。
- 30/60 秒轮询仍作为遗漏事件和无本地事件额度变化的兜底。
- 已验证：完整测试 `72 passed, 8 subtests passed`；编译检查、依赖检查和差异空白检查均通过。

## 已知边界

- 当前 Codex Desktop 的 JSONL 不总写 `task_complete`；实现以最新 `task_started` 后的 `phase=final_answer` 作为完成依据，并用本地运行日志排除陈旧的未闭合开始事件。
- 设备虽支持原厂 OTA，本项目完全禁用固件升级命令。
- 该屏的更新本质仍是“上传并切换一张完整图片”，不是局部控件刷新。
- 当前状态包只提供真实电压，不提供原厂电量百分比；电压仍用于设备状态校验，但不再渲染到界面。
